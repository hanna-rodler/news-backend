name: Trigger Action on a CRON Schedule
on:
  schedule:
    - cron: "52 * * * *" # Runs every hour at minute 55

jobs:
  call-endpoint:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger API endpoint
        run: curl -X GET https://vercel-express-test-ochre.vercel.app/orf
